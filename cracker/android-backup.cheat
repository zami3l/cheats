% android, cracker

# Décompiler le backup
run <ACTION_CMD> abe unpack <FILE_BACKUP> <FILE_OUTPUT> <PASSWORD>

$ FILE_BACKUP: ls
$ FILE_OUTPUT: echo "unpack_backup.tar," | tr ',' '\n'
$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
