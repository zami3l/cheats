% enumeration, linux, scanner

# Full énumération
run <ACTION_CMD> enum4linux -A <IP>

# Lister les partages
run <ACTION_CMD> enum4linux -S <IP>

# Lister les utilisateurs
run <ACTION_CMD> enum4linux -U <IP>

$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
