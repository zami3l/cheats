% gobuster, scanner

# Rechercher le dossier via wordlist big
gobuster dir -u <TARGET> -w <WORDLIST>

# Rechercher le dossier via wordlist small
gobuster dir -u <TARGET> -w <WORDLIST>

$ WORDLIST: ls ${pwd}/usr/share/dirb/wordlists/*