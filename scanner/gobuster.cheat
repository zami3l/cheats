% gobuster, scanner

# Rechercher les dossiers disponibles
run <ACTION_CMD> gobuster dir -u <TARGET> -w <WORDLIST>

$ WORDLIST: ls ${pwd}/usr/share/dirb/wordlists/*
$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
