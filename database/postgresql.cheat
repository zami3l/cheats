% postgresql, services

# Start/Stop/Status PostgreSQL
run <TYPE> sudo -iu postgres pg_ctl <ETAT> -D /data/postgresql

$ TYPE: echo "all,copy,view,exec" | tr ',' '\n'

% postgresql, connexion

# Connexion à PostgreSQL
run <TYPE> sudo -iu postgres psql -h /data/postgresql

$ ETAT: echo "start,stop,status" | tr ',' '\n'
$ TYPE: echo "all,copy,view,exec" | tr ',' '\n'