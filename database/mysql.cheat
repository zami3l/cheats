% mysql, databse

# Connect database mysql
run <ACTION_CMD> mysql -u <USER> -p

# Show databases
run <ACTION_CMD> mysql -u <USER> -p <PASSWORD> -e "show databses;"

$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
