% find, search

# Rechercher fichiers/dossiers in
run <ACTION_CMD> find <EMPLACEMENT> -type <FILE_TYPE> -iname "<NAME_SEARCH>" 2> /dev/null

$ EMPLACEMENT: echo "/,." | tr ',' '\n'
$ FILE_TYPE: echo "f,d,*" | tr ',' '\n'
$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
