% wordlist, search

# Rechercher une liste de mot de passe
run <TYPE> wordlistctl search <NAME_LIST>

# Téléchager une liste de mot de passe
run <TYPE> wordlistctl fetch -l <NAME_LIST>

$ TYPE: echo "all,copy,view,exec" | tr ',' '\n'