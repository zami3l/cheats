% wordlist, search

# Rechercher une liste de mot de passe
run <ACTION_CMD> wordlistctl search <NAME_LIST>

# Téléchager une liste de mot de passe
run <ACTION_CMD> wordlistctl fetch -l <NAME_LIST>

$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
