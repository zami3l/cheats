% ssh, tools

# Connexion avec file
run <ACTION_CMD> ssh -p <PORT> -i <KEY_FILE> <LOGIN>@<IP>

# Connexion simple
run <ACTION_CMD> ssh -p <PORT> <LOGIN>@<IP>

$ PORT: echo "22,2222" | tr ',' '\n'
$ KEY_FILE: ls
$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
