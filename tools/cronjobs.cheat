% cronjobs, crontab, tools

# Afficher les planifications via crontab
run <ACTION_CMD> crontab -l

# Afficher les planifications via conf
run <ACTION_CMD> cat /etc/crontab

$ ACTION_CMD: echo "exec,view" | tr ',' '\n'
